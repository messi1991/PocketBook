<!--pages/logs/index.wxml-->
<view>
    <van-cell-group
      wx:for="{{ list }}"
      wx:key="index">
        <van-cell 
            title="{{ tem.payTime }}" 
            value="{{ item.amount }}"
            label="{{ tem.remark }}" />
            
    </van-cell-group>

</view>
